---
description: 完成功能或修复后写开发记录，并和代码放进同一次提交
alwaysApply: true
---

# 开发记录

功能、修复、行为变化做完后，先在 `.mr3a-records/archived/` 写记录，再提交。记录和对应代码放在同一次提交里。用户已经要求过，不要等再次提醒。

记录用中文，照已有归档的结构：现象或需求、原因、改动、验证。不要把未完成的无关改动写进去，也不要一并提交。

`roadmap.md` 只留简短状态。测试环境和启动方式写在 `test-environment.md`，不要塞进归档。
